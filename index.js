const calc = {};

calc.add = (a, b) => {
  return a + b
};

calc.subtract = (a, b) => {
  return a - b;
};

calc.multiply = (a, b) => {
  return a * b;
};

calc.divide = (a, b) => {
  return a / b;
};

module.exports = {
  calc
};
